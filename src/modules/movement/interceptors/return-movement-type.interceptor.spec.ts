import { ReturnMovementTypeInterceptor } from './return-movement-type.interceptor';

describe('ReturnMovementTypeInterceptor', () => {
  it('should be defined', () => {
    expect(new ReturnMovementTypeInterceptor()).toBeDefined();
  });
});
